# Azure App Service startup command - SIMPLE VERSION
gunicorn --bind 0.0.0.0:8000 --workers 1 --timeout 120 chetak.wsgi:application

# Alternative commands for troubleshooting:
# python manage.py runserver 0.0.0.0:8000
# gunicorn --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --log-level debug chetak.wsgi:application

# For container deployment:
# CMD ["gunicorn", "--bind", "0.0.0.0:8000", "--workers", "1", "--timeout", "120", "chetak.wsgi:application"]